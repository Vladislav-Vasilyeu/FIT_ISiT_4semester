### **1. Что такое словарь данных Oracle? Для чего он необходим?**  
**Словарь данных Oracle** — это набор представлений (`DBA_`, `USER_`, `ALL_`, `V$` и др.), содержащих метаданные о базе данных, таких как пользователи, таблицы, индексы, привилегии, структуры схем и журналы транзакций.  
**Назначение:**  
- Позволяет администраторам и разработчикам получать информацию о состоянии и структуре БД.  
- Используется для управления доступом и оптимизации запросов.  

---

### **2. Проклассифицируйте представления словаря данных Oracle.**  
1. **DBA_*** – для администраторов базы данных (содержит полную информацию о БД).  
2. **ALL_*** – показывает объекты, к которым текущий пользователь имеет доступ.  
3. **USER_*** – информация о схемах и объектах конкретного пользователя.  
4. **V$*** – динамические представления, содержащие данные о работе базы в реальном времени.  

---

### **3. Что значит, что Oracle 12c имеет клиент-серверную архитектуру?**  
Это означает, что база данных работает по принципу **разделения задач**:  
- **Серверная часть (Server):** управляет данными, выполняет SQL-запросы и хранит их.  
- **Клиентская часть (Client):** отправляет запросы на сервер и получает результаты (например, SQL Developer, JDBC/ODBC-клиенты).  

Такой подход обеспечивает масштабируемость и удаленный доступ к данным.  

---

### **4. Что такое экземпляр (инстанс) сервера СУБД?**  
**Экземпляр Oracle (Instance)** – это набор запущенных процессов и областей памяти, необходимых для работы базы данных.  
Состоит из:  
- **SGA (System Global Area)** – глобальная область памяти.  
- **Фоновые процессы** – PMON, SMON, DBW, LGWR и др.  

---

### **5. Перечислите все этапы старта и останова инстанса Oracle 12c. Поясните их назначение.**  
#### **Этапы запуска (Startup)**  
1. **NOMOUNT** – запускается экземпляр (создаются процессы и SGA, но БД не монтируется).  
2. **MOUNT** – база данных подключается, но остается недоступной для пользователей.  
3. **OPEN** – база данных полностью открыта для работы.  

#### **Этапы остановки (Shutdown)**  
1. **SHUTDOWN NORMAL** – завершает сессию после выполнения всех текущих транзакций.  
2. **SHUTDOWN IMMEDIATE** – прерывает сессии, но откатывает транзакции.  
3. **SHUTDOWN ABORT** – аварийное завершение работы, возможны несохраненные изменения.  

---

### **6. Где указывается, что при старте операционной системы будет запущен инстанс Oracle 12c?**  
Это указывается в файле **`/etc/oratab`** (на Linux) или в **службах Windows**.  
- На Linux можно настроить автостарт с помощью **`dbstart`** и **`dbshut`**.  
- В Windows настраивается через **OracleService и OracleListener в services.msc**.  

---

### **7. Поясните назначение опции Oracle 12c Multitenant.**  
**Multitenant** – это архитектура, позволяющая одной **контейнерной базе (CDB)** управлять несколькими **подключаемыми базами данных (PDB)**.  
**Преимущества:**  
- Упрощает администрирование.  
- Повышает производительность за счет общей памяти и процессов.  
- Позволяет быстро развертывать новые базы.  

---

### **8. Поясните назначение баз данных CDB, PDB, PDB$SEED.**  
- **CDB (Container Database)** – главная контейнерная БД, содержащая общие метаданные.  
- **PDB (Pluggable Database)** – подключаемая БД, содержащая данные конкретного приложения.  
- **PDB$SEED** – шаблонная БД для быстрого создания новых PDB.  

---

### **9. Что такое общий пользователь Oracle 12c Multitenant?**  
**Общий пользователь (Common User)** – это пользователь, существующий во всей **CDB** и всех **PDB** (если не ограничен привилегиями).  
- Имя всегда начинается с **C##** (например, `C##ADMIN`).  
- Может управлять несколькими PDB одновременно.  

---

### **10. Какие табличные пространства автоматически создаются при инсталляции СУБД Oracle 12c?**  
- `SYSTEM` – системное табличное пространство.  
- `SYSAUX` – вспомогательное системное пространство.  
- `UNDOTBS1` – пространство откатов (UNDO).  
- `TEMP` – временное табличное пространство.  
- `USERS` – стандартное пространство для пользовательских данных.  

---

### **11. Какие 2 пользователя обязательно создаются при инсталляции СУБД Oracle 12c?**  
1. **SYS** – суперпользователь, владеющий словарем данных.  
2. **SYSTEM** – административный пользователь с широкими правами, но без полного контроля над БД.  

---

### **12. Какие табличные пространства дублируются в каждой PDB?**  
В каждой **PDB** создаются:  
- `SYSTEM` – для системных объектов PDB.  
- `SYSAUX` – вспомогательное табличное пространство.  
- `TEMP` – временное пространство.  
- `USERS` – стандартное пространство пользователей.  

---

### **13. Что такое консолидация баз данных? В каких случаях прибегают к консолидации баз данных?**  
**Консолидация** – объединение нескольких БД в одну **CDB** с множеством **PDB**.  
Используется, когда:  
- Нужно сократить затраты на поддержку множества отдельных серверов.  
- Требуется централизованное администрирование.  
- Есть необходимость уменьшить использование ресурсов.  

---

### **14. Какие преимущества дает Oracle 12c Multitenant?**  
1. **Экономия ресурсов** – несколько PDB используют одну CDB, снижая нагрузку на сервер.  
2. **Гибкость** – можно быстро клонировать и переносить базы.  
3. **Облегченное администрирование** – один администратор управляет сразу несколькими PDB.  
4. **Безопасность** – доступ пользователей и привилегии разграничены по контейнерам.  
5. **Масштабируемость** – поддержка множества подключаемых баз без усложнения структуры.  

---

🔥 Готово! Если нужны пояснения, пиши. 🚀